[package]
name = "kokoro-common"
version = "0.1.0"
edition = "2021"
description = "shared types for kokoro private inference"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
